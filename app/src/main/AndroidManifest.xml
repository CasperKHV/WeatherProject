<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coloreffect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        <!--android:screenOrientation="portrait"> заблокировать вертикальное положение, запретить переворачивать -->
            <!--android:configChanges="orientation|screenSize блокирует onCreate при повороте экрана и активити не пересоздаётся"-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WeatherResult">
            <!--android:screenOrientation="portrait">-->

        </activity>
    </application>

</manifest>